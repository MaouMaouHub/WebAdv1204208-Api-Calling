export interface TripGetRes {
  idx: number;
  name: number | string;
  country: number | string;
  destinationid: number;
  coverimage: number | string;
  detail: number | string;
  price: number;
  duration: number;
}
